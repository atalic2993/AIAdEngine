# OS noise
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Playwright / agent scratch output
.playwright-mcp/

# Vercel link
/.vercel

# Raw source drops. web/public/demo.mp4 is the copy the site ships,
# so the original does not need to live in git twice.
/assets/
